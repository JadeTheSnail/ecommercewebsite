class Products {

    constructor(image, name, price, desc, link){
            this.image = image,
            this.name = name,
            this.price = price, 
            this.desc = desc,
            this.link = link
    }

}


export default Products